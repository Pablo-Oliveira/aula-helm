apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Hello-Rancher-Pablo.fullname" . }}-test-connection"
  labels:
    {{- include "Hello-Rancher-Pablo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Hello-Rancher-Pablo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
