apiVersion: v1
kind: Service
metadata:
  name: {{ include "Hello-Rancher-Pablo.fullname" . }}
  labels:
    {{- include "Hello-Rancher-Pablo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Hello-Rancher-Pablo.selectorLabels" . | nindent 4 }}
